/**
 * src/models/note.model.ts
 * Purpose: Defines the Note interface structure.
 */
export interface Note {
    id: string;
    title: string;
    content: string;
    createdAt: string;
    updatedAt: string;
}
